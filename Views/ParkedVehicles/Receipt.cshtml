@model Garage2._0.Models.ViewModels.ReceiptViewModel
<!-- Display receipt details -->
<h2>Checkout receipt</h2>
<div>
    <dl class="dl-horizontal">
    <dt>Vehicle ID: </dt>
        <dd>@Model.VehicleId</dd>
    <dt>Registration Number: </dt>
        <dd>@Model.RegistrationNumber</dd>
    <dt>Arrival Time: </dt>
        <dd>@Model.ArrivalTime</dd>
    <dt>Departure Time: </dt>
        <dd>@Model.DepartureTime</dd>
        <dt>Parking Time: </dt>
        <dd>@Model.FormattedParkingTime</dd>
    <!-- Add any other receipt details here -->
    <!--Refreshing the page throws an Error 404, in future need to switch to Session ID instead of TempData-->
    </dl>
</div>

<button onclick="window.print();" class="btn btn-primary">Print</button>